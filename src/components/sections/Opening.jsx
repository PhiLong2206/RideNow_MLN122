import { motion } from 'framer-motion'; import { openingStories } from '../../data/content.js';
import SectionHeading from './SectionHeading.jsx';
import styles from './Sections.module.css';
export default function Opening(){return <section id="opening" className={`${styles.opening} section`}><div className="section-inner"><SectionHeading eyebrow="01 · Vấn đề mở đầu" title="Một cú chạm, nhiều sự phụ thuộc"/><div className={styles.sceneList}>{openingStories.map(({icon:Icon,title,text},i)=><motion.article key={title} initial={{opacity:0,y:55}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.45}} transition={{duration:.7}}><span>0{i+1}</span><Icon/><h3>{title}</h3><p>{text}</p></motion.article>)}</div><motion.blockquote className={styles.question} initial={{opacity:0,scale:.96}} whileInView={{opacity:1,scale:1}} viewport={{once:true}}>“Khi một nền tảng ngày càng tiện lợi, quyền lực thị trường có thể tập trung đến mức nào?”</motion.blockquote><p className={styles.quietNote}>Video tình huống có thể được nhóm trình chiếu riêng bên ngoài website trước khi đi vào phần lý thuyết.</p></div></section>}
